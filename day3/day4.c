#include <stdio.h>

// macro // #define  /// create always globally level
// assignments operators += -= /= %= *= 

// conditions operators  < , > , <=  , >= , !  , != ,==      !mtlb  "not"
#define PI 3.14 // constant

int main()
{

  int a;
  int b = 15;
  int c = 10;   // example b<c  =   false   b>= c = true // !true = false  //   5 == 5 (true)  // 5!=5(false)

  // b = b + 5;

  b -= 5; // short hand
  printf("b value is = %d\n", b);
  printf("Enter your a value inp\n");
  // scanf("%d", &a); // &=> address of a // scanf is used for user input

  return 0;
}

// arthmetic operators => + - / * % modulus

// assignment operators  => =


///
// scanf // user sae input taking 

//Q1 Find area square = side * side
//q2  find area of rectangle = l*b
//q3 find area of triangle = > 1/2  * b *h
//q5 find simple intrest = > (p * r * t)