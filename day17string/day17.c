#include <stdio.h>
#include <string.h>

int main()
{

  // char a[]= {'r','a','h','u','l'};
  // char a[] = "rahul";

  // int length = strlen(a);

  // // char result[5] = strupr("hello");
  // // printf("%s", result);

  // printf("%d\n", length);

  // printf("%s", a);

  // char a[5];

  // strcpy(a,"rahul");

  // printf("%s",a);

  // "hello" + " " +"world" = "hello world" concatinate

  // char result[10] = strcat("hello", "world");
  int num = strcmp("a", "a");

  printf("%d", num);

  // printf("%s", result);
  return 0;
}
